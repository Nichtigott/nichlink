[workspace]
members = ["core", "build", "cli", "debug", "studio", "plugin-host", "mcp"]
resolver = "2"


[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.96.0"
license = "MIT"

[profile.release]
lto = "thin"
codegen-units = 1
