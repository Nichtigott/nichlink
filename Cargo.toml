[workspace]
members = ["core", "build", "debug", "studio", "plugin-host", "mcp"]
resolver = "2"


[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
license = "MIT"

[profile.release]
lto = "thin"
codegen-units = 1
