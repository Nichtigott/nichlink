# Third-party audit, 2026-09-25 — findings index

Six independent, adversarial angles reviewed `d72c703` with read-only access; each
measured what it claims (mutations were applied to `git archive` snapshots, never to
the checkout) and labelled evidence `[实测]` / `[代码]` / `[报告]`. Reports live in
the session; this file is the index they are worked from, so a later round does not
need to re-derive them. Base of every angle: nine crates published at `0.1.0`.

六路独立对抗性审查在 `d72c703` 上只读进行;每条都实测自己的主张(变异只作用于
`git archive` 快照,从不碰检出),证据标为 `[实测]`/`[代码]`/`[报告]`。报告本体在会话里,
本文件是它们的工作索引,使后续轮次不必重新推导。每路的前提:九个 crate 已发 `0.1.0`。

Angles: `surfaces` (CLI/MCP/Studio), `kernel` (parsers/scanners), `plugin-host`
(wasm/trust/process), `gates` (`conventions/`), `consumer` (a stranger's first
contact with the published crates), `release-ci` (release/packaging/CI).

## Fixed in this round / 本轮已修

| id | what | pin |
| --- | --- | --- |
| K1 | The nesting guard reset its chain counter on `,`, and `X<u8, X<u8, …>>` has a comma at every level, so the guard admitted ~1024 generic levels while `syn` overflowed the stack at ~170 and **aborted the process**. Angle brackets are now measured directly (`Shape::Arguments`, limit 128). | `deep_input_tests::the_refusal_names_the_limit_that_applies` asks the guard alone, so the red half is an assertion failure rather than an abort — verified red by disabling the counter. |
| K2 | Three authoring parsers handed manifest/editor strings to `syn::parse_str` with no guard; a **542-byte** `flow_provider` value aborted the process. All three now call `guard_nesting` first. | `flow_tests::a_deep_type_path_is_refused_not_fatal`, `::a_deep_flow_expression_is_refused_not_fatal`. |
| S1 | Studio delete renamed `source.parent()`, so a generated face flattened to `src/widget.rs` moved **all of `src/`** into the trash. The guard migration already had is now on delete. | `studio::app::tests::edit::delete_refuses_a_face_outside_its_own_directory`. |
| C2 | `run_method/tests/external_compact_face.rs` asserted the workspace-relative spelling, so the crate's own suite failed for anyone who extracted the published tarball. It now asserts the portable *tail*. | the test itself, run from an extraction. |
| G5 | `crate_directories` descended one level, so a member under a non-crate parent was invisible to every gate. It now derives from the root manifest's `members`. | `conventions::tests::{a_deeply_nested_member_is_a_crate_directory, the_members_array_ends_at_its_bracket, a_manifest_without_members_is_a_panic}`. |
| G6 | The size ceiling measured only `src/`, so a crate-root `build.rs` could grow without limit, and a `_tests` *name* exempted a file. `build.rs` is measured; the name only exempts when the file is mounted behind `#[cfg(test)]`. | `size::tests::{the_crate_root_build_script_is_measured, an_unmounted_test_shaped_file_is_measured, a_mounted_test_file_is_exempt}`. |
| C1 | The `prototype-fixtures` tests resolved `studio/tests/fixtures/node-editor`, which cargo does not package (a nested package), so the published `nichlink-studio` failed 26 tests under `--all-features`. The fixture is now resolved through `node_editor_fixture()`, which skips the test when the fixture is absent — the contract `prototype-fixtures` names is a checkout contract, as `AGENTS.md` already said. | extracting the newly packed `.crate` and running `cargo test --all-features`: 95 passed, 26 fixture failures gone. One failure remains and is release skew, not code: the packaged studio resolves the *published* `nichlink-run-method 0.1.0`, which predates this round's delete guard, so `delete_refuses_a_face_outside_its_own_directory` cannot see it until 0.1.1 ships. |
| C7 | `core/tests/nesting_budget.rs` ships in the package and walked `CARGO_MANIFEST_DIR/..`, so from a registry unpack it either panicked (87 files < the >100 floor) or "passed" by scanning whichever unrelated crates were unpacked beside it. It now checks that the parent is a NichLink checkout and stands down with a printed note otherwise. | extracting `nichlink-core` alone into an empty directory and running it: the skip fires and the suite is green — verified with `--nocapture`. |
| C6 | Four relative links in packaged READMEs pointed at files no package contains (`../docs/graft.md` ×3, `../examples/…/health_check.rs`, and the Studio screenshot). They are absolute GitHub URLs now, which is what a crates.io reader can actually follow. | none (docs); `doc_anchors` does not resolve prose links. |
| P4/P7/P8 | `WasmBackend::load`'s rustdoc claimed the ABI export is required (the README's contract is "optional, legacy when absent"), said nothing about enforcing no slot policy, and `max_input_bytes` was documented as the accepted ceiling although the input is written into the plugin's *initial* memory. All three now say what the code does. | none (docs). |
| — | The size ratchet's own prose claimed the exception list "holds twelve today" while `BASELINE` held nine; the count is no longer restated (it is `BASELINE.len()`). | none (docs). |
| G9 | The markdown half of `doc_blocks` honoured no fence tag while the doc-comment half honoured `macro-input`, so the repository's own sanctioned escape hatch was reported as a broken block; it also recognised only ``` fences whose language was exactly `rust`. It now reads ``` and `~~~`, accepts `rust`/`rs`/`Rust`, honours `macro-input`, and its tests moved to `doc_blocks_tests.rs` to stay under the ceiling. | `doc_blocks::a_sanctioned_tag_and_every_fence_spelling_are_read` (sanctioned tag green, `~~~` and `rs` red) — the two false negatives were measured by the delegated review. |
| — | Two gate false positives fixed: a single-quoted TOML value (`name = 'x'`, valid TOML) was rejected by `naming`, and a line under `#[cfg(any())]` (the workspace's never-compiled idiom) was reported by `purity`. | the shipped-tree checks stay green both ways; the fixtures in `naming`/`purity` cover the accept side. |
| G7+G8 | The release-workflow gate trusted step *names* and a `contains("refs/tags/")` substring, and read only `release.yml`: an upload under another name (or none) was invisible, `!startsWith(github.ref, 'refs/tags/')` passed while publishing on every non-tag ref, and a second branch-triggered workflow produced no finding. The gate now selects steps by the command they run (`--publish` / `cargo publish`, comments skipped), requires the positive tag test and rejects `!startsWith`/`!=`/`||`, reads **every** workflow in `.github/workflows/`, and reads a folded `if: >-` condition as one condition (the old gate reported a correctly guarded step). | `release_workflow::{a_negated_tag_test_is_reported, the_upload_command_matters_not_the_step_name, a_second_workflow_is_checked_too, a_folded_positive_guard_is_accepted}` — the red half is the delegated measurement of the old gate, which was green for all four shapes. |
| K5 | The JSONL writer emits raw UTF-8 while the reader rejected every byte ≥ 0x80 *and* every `\u` escape, so no encoding of a non-ASCII symbol could be read back — and the refusal message told the producer to escape. The reader now decodes raw multi-byte UTF-8, `\uXXXX` (including a surrogate pair), and the `\/`, `\b`, `\f` escapes JSON allows. | `jsonl::tests::a_non_ascii_symbol_round_trips` (raw form, writer→reader round trip, `\u00e9`, `\ud83d\ude00`) — red before. |
| S2 | MCP `read` computed `center + context` from an unbounded caller `line`: debug panicked, release returned `start > end` with `isError:false`. The centre is clamped to the file and the arithmetic saturates. | `tools::tests::a_huge_line_number_still_yields_a_forward_range` (`u64::MAX`, `usize::MAX`, 1) — red before. |
| S3 | `cli/src/lib.rs::main` used `std::env::args()`, which *unwraps*, so any non-UTF-8 argv (a legal Linux path) panicked with a backtrace in every subcommand. `argv_strings` converts `args_os()` and names the offending argument; both binaries use it. | `cli::tests::a_non_utf8_argument_is_refused_by_name` (Unix) — red before. |
| S4 | `nichlink new --path <dir>` accepted any directory, wrote a manifest with an unresolvable `path` dependency and exited 0. The command now resolves the path and requires `core/`+`build_method/`+`run_method/` before writing anything. The predicate lives in the CLI, not in `build_method`: the packaged CLI must compile against the *published* `nichlink-build-method`, and a new cross-crate symbol would break the package audit until the next release (measured: `cargo package` of `nichlink-cli` failed exactly that way before the predicate moved). | `new_command::tests::only_a_real_checkout_is_accepted`, plus an end-to-end run: `--path /tmp` → "is not a NichLink checkout", no directory created; `--path <checkout>` → created. |
| K3 | `mask_non_code` had no raw-string rule, so `r#"…"#` with an interior quote leaked the rest of the literal as code: braces closed bodies early, `fn` inside a JSON payload declared a phantom symbol, and calls inside it appeared in the call graph (MCP index + Studio). The scanner now recognises `r`/`br`/`cr` + N `#` and masks through the matching `"` + N `#`; it moved to `source/lex.rs` because `source.rs` had reached the ceiling. | `source_tests::raw_strings_do_not_leak_code` (range, phantom `fn`, phantom call, `br` prefix) — red against the old masker. |
| K4 | `function_source_range` tested the start line on raw text and matched the name as a substring: `// fn ghost() {` started a range, and `new` matched `fn renew(`. The predicate now runs on masked text and requires a whole identifier. | `source_tests::a_phantom_function_range_needs_real_code` — red before. |
| K6 | `registration_kinds` read `kind:` out of comments (`// kind: Ghost` → `["Ghost"]`). It now scans masked text. | `source_tests::registration_kinds_come_from_code_only` — red before. |
| C3+C4 | The scaffold wrote internal requirements with no `version` (so `cargo publish` refused the generated manifest) and no `[workspace]` table (so it could not be built inside an existing workspace). Both arms now carry `version = "0.1.0"`, and the template opens with `[workspace]`. | `scaffold::project::a_generated_project_can_be_published_and_built_in_a_workspace` — red before. |
| G3 | `lint` searched only `#[allow(` and stopped at the first `)`: a crate-root `#![allow(missing_docs)]`, a second attribute on the same line, and `#[cfg_attr(all(), allow(missing_docs))]` all passed, while the stronger `#![deny(missing_docs)]` was wrongly refused. It now finds `allow(` anywhere (needle assembled at run time so the gate does not scan itself), scans to the *matching* paren, keeps an identifier boundary, and accepts warn-or-stronger declarations. | `lint::{crate_root_allows_and_second_attributes_are_violations, a_stronger_or_multi_lint_declaration_is_accepted}` — both red against the old search. |
| G4 | `mounting` searched each line for the literal `include!` followed by a delimiter: `include ! (…)` and a comment between the name and the `!` both spliced a module unseen. It now searches the whole masked file for `include` + optional spaces + `!`. | `mounting::a_splice_is_seen_however_it_is_spelled` (space, next-line delimiter, comment, brace) — red against the old search. |
| R1 | The tag check and `--check-table` read the release version from the *first* `version = "…"` line of the root manifest, so a stale centralized requirement above `[workspace.package]` let a tag pass while `cargo publish` uploaded a different version. All three readers now take the section, and `release_version.rs` gates the invariant. | `release_version::{a_version_line_before_the_release_section_is_reported, a_member_with_its_own_version_is_reported, a_multi_line_requirement_with_a_wrong_version_is_reported, a_published_requirement_without_a_version_is_reported, the_shipped_manifests_name_one_version}`; shell red/green: old read `0.1.0`, new read `0.1.1` on the mutant. |
| G1+G2 | `purity` scanned each masked line literally, so four measured spellings were green: a brace import split across lines, `std :: fs`, a path split across a newline, and `use std as s;` + `s::fs`. It now searches a whitespace-free copy (a path is a token stream, not a line), expands `std::{…}` with the line map kept, resolves `std` aliases, and includes `env!`/`option_env!` behind an identifier boundary. | `purity::{the_ways_a_path_can_hide_from_a_line_scan_are_violations, an_aliased_std_is_still_std}` — the alias pin is red against the newline-preserving behaviour. |
| — | `docs/audit-production-readiness.md` m1/m2/m3 rows, `CHANGELOG.md`, both root READMEs: release state is now "published 2026-09-25", the version line is recorded as **0.1.x** (decided 2026-09-25), and the "fourteen internal requirements" count is gone (measured 45). | n/a (docs). |

## Queue / 待做

Ordered by severity, then by how much damage a silent failure does. Each item names
its angle id, the file, the smallest fix, and the pin a fix must turn red.

### CRITICAL

- **G1+G2 (gates) — purity escapes.** `purity::findings` scans each masked line for
  literal `std::fs`/`std::env`, so four independent bypasses were measured green on the
  load-bearing architectural rule: `use std::{\n env,\n fs,\n};` (both braces on one line
  only), `use std as s;` + `s::fs::metadata(…)`, `std :: fs :: metadata(…)` (spaces around
  `::`), and `option_env!("HOME")`/`env!`. Fix: scan the whole masked file as one string,
  collapse whitespace around `::`, handle multi-line brace imports (or walk `UseTree`s with
  `syn`), add `env!`/`option_env!`. Pin: a fixture with all four shapes must be red.
- **G3 (gates) — `lint` misses crate-root `allow`.** Keeping `#![warn(missing_docs)]` and
  adding `#![allow(missing_docs)]` disables the lint (last attribute wins) and passes;
  `#[allow(dead_code)] #[allow(missing_docs)]` on one line passes (the scan stops at the
  first `)`); `#[cfg_attr(all(), allow(missing_docs))]` passes. Fix: match `allow(` anywhere
  in masked text, scan to the *matching* paren, accept `warn`/`deny`/multi-lint forms,
  reject `allow`/`forbid` naming `missing_docs`. Pins: the three shapes above.
- **G4 (gates) — `mounting` misses `include !`.** `include ! ("x")`, a newline between
  `include!` and its delimiter, and `include/* c */("x")` all splice a module while the gate
  counts one `include!`. Fix: scan the whole masked file for `include` + optional whitespace
  + `!`. Pin: `pub mod spliced { include ! ("zz_body.rs"); }` must be red.
- **C1 (consumer) — published `nichlink-studio` fails its own `--all-features` suite.**
  26 `prototype-fixtures` tests resolve `studio/tests/fixtures/node-editor`, which is not in
  the tarball, while `studio/Cargo.toml` advertises the feature. Fix: move the fixture host
  under `studio/src/` (or package it), or stop advertising the feature as published. Pin:
  extract the package and `cargo test --all-features` must exit 0.

### MAJOR

- **P1 (plugin-host) — the signature does not bind the registration.** `signing_payload`
  covers the manifest + bytes but not `RegistrationInfo`, so after tampering
  `registration.flow`/`parent` a `Signature`-assured artifact still verifies, passes a
  contract check the honest artifact fails, and runs. Fix: include a canonical digest of
  `RegistrationInfo` in `signing_payload` (or sign the declaration) and verify it. Pin: sign,
  flip `flow`/`parent`, both verifiers must fail.
- **K3 (kernel) — `mask_non_code` has no raw-string rule.** `r#"…"#` with an interior quote
  leaks the rest of the literal as code; braces, `fn` and calls inside raw strings become
  real to the index and call graph (MCP + Studio). Fix: at `r`/`br`/`cr` + N `#` + `"`, skip
  to the matching `"` + N `#`. Pin: `direct_calls("let s = r#\"{\"k\": \"drop()\"}\"#;", "f")`
  must be empty.
- **K4 (kernel) — `function_source_range` starts on the raw line.** A comment or string
  containing `fn name(` starts a phantom range, and the name test is a raw substring
  (`new` matches `renew(`). Fix: evaluate the start test on `mask_non_code(line)` and require
  a non-identifier boundary. Pin: `["// fn ghost() {", "fn real() {", "}"]` → `None`.
- **K5 (kernel) — JSONL cannot round-trip non-ASCII.** The writer emits raw UTF-8; the reader
  rejects every byte ≥ 0x80 *and* every `\u` escape, so no encoding of `crate::héllo` is
  readable. Fix: decode raw multi-byte UTF-8 and implement `\uXXXX`. Pin: `from_jsonl(to_jsonl(g)) == g`.
- **S2 (surfaces) — MCP `read` overflow.** `(center + context)` overflows: debug panics,
  release returns `start > end` with `isError:false` — a wrong answer an agent would trust.
  Fix: saturating arithmetic plus a bound on `line`. Pin: `line: u64::MAX` never inverts.
- **S3 (surfaces) — CLI panics on non-UTF-8 argv.** `std::env::args()` unwraps; a legal Linux
  path with a 0xFF byte panics with a backtrace in every subcommand. Fix: `args_os()` and a
  clean error. Pin: spawn the binary with a non-UTF-8 argument.
- **S4 (surfaces) — `new --path <dir>` skips checkout validation.** An empty/nonexistent
  directory is accepted, exits 0, and writes a manifest with a path dependency that cannot
  resolve. Fix: apply the same predicate auto-detection uses (`core`+`build_method`+
  `run_method`). Pin: `new --path <empty dir>` fails and creates nothing.
- **C3 (consumer) — the scaffold cannot be published.** The `Local` dependency specs carry
  no `version`, so `cargo publish --dry-run` on a scaffolded project fails manifest
  verification. Fix: add `version = "0.1.0"` (the `Git` arm already does). Pin: scaffold →
  `cargo publish --dry-run` gets past verification.
- **C4 (consumer) — the scaffold cannot build inside a workspace.** The template writes no
  `[workspace]`, so scaffolding inside an existing workspace fails `cargo metadata`, `check`
  and `build` (the rehearsal script patches this by hand). Fix: emit `[workspace]`. Pin:
  scaffold inside a workspace with `members` and build.
- **G7 (gates) — `release_workflow` reads one file.** A second workflow can publish from a
  branch. Fix: run `findings()` over every `.github/workflows/*.yml` and flag any `run:`
  containing `--publish`/`cargo publish` without a tag guard, whatever the step is named.
  Pin: a second workflow with a branch-triggered `--publish` must be red.
- **G8 (gates) — the tag guard is a substring test.** `!startsWith(… 'refs/tags/')` passes
  (it contains the text), and a decoy step name lets the real upload go unguarded. Fix:
  require the positive conjunct and key on the step that runs `--publish`, not on its name.
  Pins: both bypasses.
- **G9 (gates) — `doc_blocks` rejects its own escape hatch and misses fences.** A
  ```` ```rust,macro-input ```` fence (the sanctioned tag, used by the audit docs) is reported
  as broken; `~~~rust` and ```` ```rs ```` fences are invisible; `hello: world` is accepted as
  struct fields. Fix: pass the tag list into the markdown handler, honour `macro-input`,
  accept `~~~`/`rs`/`Rust`, tighten `as_fields`. Pins: the tag must be green, the tilde fence
  red, the prose red.
- **G10 (gates) — no gate for the bilingual doc convention**, nor for "new pure logic is
  registered in `core/src/registry_core.rs`", nor for shim re-exports. An English-only
  `///` on a public item is invisible to rustc, clippy and every gate, and ships to docs.rs.
  Fix: a `doc_language` gate (CJK line required after the English lines on every `pub` item
  in the nine non-example crates), a registration gate, a shim gate. Pin: an English-only
  public doc must be red; an unregistered `core/src/registry_core/zzmodule/zzmodule.rs` must
  be red.
- **C5 (consumer) — the published 0.1.0 READMEs are stale** (`TraceMode::from_env`, missing
  `json` row, missing `snippets` row) because the README is part of the artifact and the
  release was cut before those fixes. Immutable; ships in 0.1.1. Record only.

### MINOR

- **K6** `registration_kinds` reads `kind:` out of comments (`// kind: Ghost` → `["Ghost"]`).
- **P2** `PluginPolicy::accepts` hardcodes `catalog=None`, so a self-declared `Official`
  manifest with a fake signature is `Accepted`; the lock check is unreachable in-tree.
- **P3** the process channel reads the whole artifact before comparing its length (2 GiB
  sparse file → 2 GiB peak RSS).
- **P4** `WasmBackend::load`'s rustdoc says the ABI export is required; the README (the
  intended contract) says optional, and the code treats it as optional.
- **P5** a failed lazy activation is invisible to readiness polling and later calls serve the
  previous generation (observability, not control).
- **P6** `SlotState::install` allocates the generation before taking the lock, so the returned
  generation can lose to a lower one.
- **P7** `max_input_bytes` is not the accepted ceiling: input is written into the plugin's
  *initial* memory, so a 1-page plugin stops at 64 KiB while the doc says 1 MiB.
- **P8** `WasmBackend::load` is public and enforces no slot policy; its rustdoc does not say so.
- **P9** process plugins inherit env/cwd/fs/network; the README disclaims a sandbox, the threat
  model's residual risk does not mention it.
- **S5** `nichlink studio` discards its arguments yet tells the user to pass a path; a non-TTY
  run dies with a raw `os error 6`.
- **S6** Studio's MIR surface hardcodes `--lib`, so it cannot inspect a binary-only host
  (including `nichlink new`'s default output).
- **S7** `check --json`/`grafts --json` emit no JSON document when package resolution fails.
- **S8** MCP replies to JSON-RPC notifications and collapses a batch array into one error.
- **C6** four relative links in packaged READMEs point at files no package contains.
- **C7** `core/tests/nesting_budget.rs` ships and walks `CARGO_MANIFEST_DIR.parent()`, so it is
  false by construction for a consumer (87 files < the >100 floor).
- **C8** `prototype-fixtures` is advertised but its contract cannot exist for a consumer.
- **C9** `run_method/README.md`'s graft excerpt implies `Result<_, String>` where the real
  signature returns `RegistryResult<Self>`.
- **C10** `AGENTS.md`'s "re-exports the module hierarchy plus a whitelist" describes flat
  globs; the practical impact is nil, the sentence is inaccurate.

### Gate false positives to fix with the same edit

`#![deny(missing_docs)]` and `#![warn(missing_docs, other)]` are rejected (stronger is
refused); a correctly guarded YAML folded condition (`if: >-`) is rejected; a single-quoted
TOML value (`name = 'nichlink-conventions'`, valid TOML) is rejected; `#[cfg(any())] use
std::fs;` (provably dead) is reported by purity.

## release-ci / 发布与 CI

- **R1 CRITICAL — the tag precondition and `--check-table` read the release version from
  the *first* `version = "…"` line of the root manifest and never read a member's resolved
  version.** Members all use `version.workspace = true`, which `version_problems`
  (`tools/nichlink-publish:323`) skips, and `workspace_requirement_problems` (`:370`) reads
  only the non-dotted `[workspace.dependencies]` section. Measured mutant: a centralized
  `[workspace.dependencies.nichlink-core] version = "0.1.0"` above
  `[workspace.package] version = "0.1.1"` → the tag step reads `0.1.0`,
  `--check-table` says "matches the manifests (9 crates)" exit 0, and `cargo publish`
  would upload **0.1.1** while the index wait polls for 0.1.0 — failing *after* the
  irreversible upload. Fix: read the version from the `[workspace.package]` section only,
  and check resolved versions/requirements via `cargo metadata --no-deps`. Pin: a
  `conventions` gate over the shipped manifests (shell-only pin impossible: these tools have
  no test harness).
- **R2 MAJOR — a multi-line inline table hides a wrong internal requirement.**
  `/^nichlink-[a-z-]+ *=/` and the `version` match are tested on the *same line*
  (`tools/nichlink-publish:348-357`), so `nichlink-core = {\n path = …, version = "0.2.0", }`
  passes while the inline and dotted spellings are refused. Fix: read resolved requirements
  from `cargo metadata`. Pin: the multi-line mutant must exit non-zero.
- **R3 MINOR — `is_published` ignores `"yanked"`** (`tools/nichlink-publish:175-188`,
  `tools/nichlink-package-audit:164-177`, byte-identical copies), so a yanked version counts
  as published and is reported `verified`. Fix: require `"yanked":false` on the matching line.
- **R4 MINOR — `for level in $levels` word-splits**, so the documented one-line-per-level
  batching never happens; a crate that does not reappear stops the release before its
  mutually independent siblings are attempted (`:518`). Fix: iterate lines.
- **R5 MINOR — both index-dependent gates conflate "index unreachable" with "not yet
  published"**: against a dead endpoint the package audit reports `verified: <1>` /
  `skipped: <8>` and **exit 0**, and the same for the dry-run publish. Fix: distinguish
  transport failure from 404 and exit non-zero when the probe could not run.
- **R6 MINOR — `tools/nichlink-package-audit` derives deps with awk/sed**, disagreeing with
  `--check-table` about the dotted and target-specific spellings, and never reads an inherited
  `readme`, `license-file`, `include`/`exclude`. Fix: derive from `cargo metadata`.
- **R7 MINOR — the `conventions` release-workflow gate matches hard-coded step names**, so an
  upload step with another name (or no `name:`) yields no finding — the regression detector
  cannot detect the regression (see also G8). Fix: select steps by the command they run.
- **R8 MINOR — `core/tests/nesting_budget.rs` ships and walks `CARGO_MANIFEST_DIR/..`**, so
  from a registry unpack it "passes" by scanning whichever unrelated crates are neighbours
  (measured: 294 files from nine extracted crates). Fix: take the root from an env var and
  skip when unset, or move the walk into `conventions`.

## Continuation pass / 续做的一轮

Every remaining item below was fixed in one pass, smallest change first, each with the
measurement named. "Not reproducible" means the claim was re-measured against the current
tree and did not survive; those are recorded as such rather than "fixed".
下面每一条剩余项都在同一轮里修完，改动尽可能小，并注明实测方式。"不可复现"指该主张对着当前树
重新实测后不成立，这类记为"不可复现"而不是"已修"。

| id | what | pin / measurement |
| --- | --- | --- |
| P1 | The signature did not bind the `RegistrationInfo` that travels in the same artifact: tampering `registration.flow`/`parent` still verified as `Signature`. `PluginManifest::signing_payload` now takes the registration and appends a canonical encoding of every field; `PluginTrustPolicy::verify_with` takes it too, and `PluginSignatureVerifier::verify` receives the canonical payload the kernel built instead of the raw bytes, so a host cannot reassemble the message and drop a field. | `artifact_tests::a_tampered_registration_no_longer_earns_a_signature` — red by disabling the registration coverage in `signing_payload` (failed on the moved `parent`). |
| P2 | `PluginPolicy::accepts` hardcoded `catalog=None`, so the official lock check was unreachable for every caller. It now takes the catalog. | `plugin_policy::the_official_lock_check_is_reachable_through_accepts`. |
| P3 | The process channel read the whole artifact before comparing its length, so a 256 MiB mismatch cost 256 MiB of peak allocation. The length is compared first. | `plugin-host/tests/process_load_cost.rs` (counting allocator): 268 435 478 bytes peak without the fix, under 1 MiB with it. |
| P5 | A failed lazy activation was invisible: the previous generation kept serving and `is_loaded` stayed `true`. `WasmPluginTable::activation_error` reports it, cleared by a successful activation. | `fault_matrix::a_failed_activation_is_observable` — red with the record disabled. |
| P6 | `SlotState::install` allocated the generation before taking the lock, so a racing install could store a lower generation last. The allocation moved under the lock. | `fault_matrix::the_highest_generation_is_the_one_that_stays_pending` — red on round 0 with the pre-lock allocation (pending 6 vs highest 8). |
| P9 | The threat model's residual risk said nothing about process plugins inheriting env/cwd/filesystem/network. | docs; the paragraph now names the inheritance and says the confinement has to come from outside. |
| C8 | `prototype-fixtures` was advertised without saying its fixture cannot exist for a consumer. | docs in `studio/Cargo.toml` next to the feature. |
| C9 | Claimed `run_method/README.md` implies `Result<_, String>` where the real signature returns `RegistryResult<Self>`. | **Not reproducible**: the only Rust fence in that README is the `health_check` example, and the graft paragraph claims only that failures are `Err`, which matches `load_graft_record -> Result<GraftPlanDocument, String>`. |
| C10 | `AGENTS.md` and `core/src/lib.rs` said the crate root re-exports "the module hierarchy plus a whitelist" and that anything else "is reached through its module" — but the kernel's modules glob into the root, so everything is reachable there. | docs; reworded to say the module path is the official address and the curated names are the promised bare ones. |
| S5 | `nichlink studio` discarded its path argument and died with a bare `os error 6` on a non-TTY. It parses and honours one path (and `--help`), and Studio names the offending stream. | `cli::studio_honours_its_path_argument`, `studio/tests/launch.rs::redirected_output_fails_with_a_message_instead_of_an_errno`; shell runs recorded in the subagent report. |
| S6 | Studio's MIR surface hardcoded `--lib`, so a binary-only host could not be inspected. It falls back to `--bins` on cargo's "no library targets". | `studio::mir_inspection_resolves_the_target_a_package_actually_has` (binary-only, lib-only, both). |
| S7 | `check --json`/`grafts --json` wrote no document when resolution failed. They now write the command's JSON shape with the failure in it, and return the error. | `cli::check_json_reports_a_resolution_failure_as_json`, `::grafts_json_reports_a_resolution_failure_as_json`. |
| S9 | Found while fixing S7: `explain --json` / `explain --overlay --json` had the same gap. | `cli::explain_json_reports_a_resolution_failure_as_json`, `::explain_overlay_json_reports_a_resolution_failure_as_json`. |
| S8 | The MCP bridge replied to notifications and collapsed a batch array into one error. A request without `id` now gets no reply, and an array is dispatched element-wise. | `mcp::a_notification_gets_no_reply`, `::a_batch_gets_one_reply_per_element`. |
| R3 | `is_published` ignored `"yanked"`, so a yanked version counted as published in both tools. | fixture index bodies, `yanked:true` vs `false`, old vs new helper (subagent measurement). |
| R4 | `for level in $levels` word-split the level table, so the documented per-level batching never happened and a crate that did not reappear stopped the release before its siblings. Now iterated by line. | fixture table (3 crates on one line) + a publish-mode run with a cargo shim. |
| R5 | Both index-dependent gates reported "not published" from an unreachable index and exited 0. Only HTTP 404 stays "not published"; transport failure exits 1. | dead endpoint (`http://127.0.0.1:9`): old exit 0, new exit 1 with the reason; 404 path unchanged. |
| R6 | The package audit derived deps and the declared README with awk/sed, missing dotted/target-specific spellings and inherited readme/license-file. Now derived from `cargo metadata --no-deps`. | mutated `/tmp` copy with `[dependencies.nichlink-core]` + `readme.workspace = true`; checked against `jq` on all 12 packages. |
| R9 | Found while fixing R3–R6: `sub(/".*$/, "", value)` left the trailing quote under mawk, so `--check-table` failed there. Now `sub(/"$/, "", value)` in the three version readers (the fourth site is a different, correct use). | `--check-table` under gawk unchanged; the pattern is the standard trailing-quote strip. |
| G10 | Four rules had no gate: the bilingual `///` convention, "new pure logic is registered in `core/src/registry_core.rs`", the execution-surface shim re-exports, and `naming`'s cross-check against the templates and CI. Added as: `bilingual` (a new gate), `mounting::mounts` (the kernel's module tree), `shims` (a pinned ratchet over the 17 re-exports), and `naming::referenced_names` (dependency keys and `-p` arguments). | `bilingual::the_shipped_tree_documents_in_both_languages` (first run found 19 drifted blocks); `mounting_tests::the_shipped_kernel_mounts_every_module_file` (red with `core/src/registry_core/forgotten.rs`); `shims` (red with `pub use nichlink::tree;` deleted); `naming::a_reference_to_a_package_that_does_not_exist_is_reported`. |
| — | The size ratchet's Chinese prose still claimed "这份清单今天有 12 项" against nine entries (the English half had been fixed, the Chinese half had not). | prose no longer restates the count. |

Two claims did not survive re-measurement and are **not** fixed because there is nothing
to fix: C9 above, and the "README's `Overlay` return type" minor — no root README mentions
`Overlay` any more, and `docs/audit-graft-vs-readme.md`'s C7 row records the claim as MET.
两条主张重新实测后不成立，因此**没有**可修之处：上面的 C9，以及"README 的 `Overlay` 返回类型"
这条 MINOR——根 README 已不再提及 `Overlay`，而 `docs/audit-graft-vs-readme.md` 的 C7 行把该
主张记为 MET。

## Handoff: what is left, and how to start / 交接：剩下什么、怎么开始

The functional queue is empty: every CRITICAL, MAJOR, and MINOR in this file is fixed,
measured, or recorded as not reproducible, and the version line has moved to **0.1.1**
(workspace version + every internal requirement + `CHANGELOG`). What remains is
decisions, not defects.
功能队列已清空：本文件里的 CRITICAL、MAJOR 与 MINOR 全部已修、已实测，或记为不可复现，版本线
也已推进到 **0.1.1**（工作区版本 + 每一处内部要求 + `CHANGELOG`）。剩下的是决定，不是缺陷。

1. **The release — done (2026-09-26).** `v0.1.1` was tagged and pushed; the release
   workflow (run `36206756375`) ran every check, published all nine crates in dependency
   order, and its last step built a throwaway consumer outside the checkout and resolved
   all nine by version. All nine `0.1.1` lines are on the index, and
   `tools/nichlink-package-audit` now reports `verified:` all nine with `skipped: none`,
   so both the designed waiting state and the release skew the audit recorded (a packaged
   `studio` suite that resolved the published `run_method 0.1.0`) are gone. **`v0.1.3`
   followed the same way (run `36236583886`)**: the tag named the workspace version, every
   gate ran, all nine crates went up in dependency order, and the last step resolved all
   nine by version. `0.1.2` was never published, so its changes shipped in `0.1.3`;
   `tools/nichlink-package-audit` reports `verified:` all nine with `skipped: none`.
   **发布本身——已完成（2026-09-26）。** `v0.1.1` 已打 tag 并推送；发布工作流（run
   `36206756375`）跑完全部检查、按依赖顺序发布九个 crate，最后一步在本检出之外构建一次性
   消费者并按版本解析到全部九个。九个 `0.1.1` 都在 index 上，`tools/nichlink-package-audit`
   现在报告九个全部 `verified`、`skipped: none`——设计中的等待态与审查记录的发布错位（打包后的
   `studio` 套件解析到已发布的 `run_method 0.1.0`）都消失了。**`v0.1.3` 以同样的方式跟进
   （run `36236583886`）**：tag 与工作区版本一致、全部门禁跑过、九个 crate 按依赖顺序上传、
   最后一步按版本解析到全部九个。`0.1.2` 从未发布，它的改动随 `0.1.3` 一起走；
   `tools/nichlink-package-audit` 报告九个全部 `verified`、`skipped: none`。
2. **Studio's destructive-action UX** (keep a copy instead of deleting, an undo, or a
   confirmation) and whether the hand-drawn canvases in `studio/src/studio/ui/graph/`
   should be deleted — both are product decisions the earlier rounds surfaced and
   deliberately left alone.
3. **Wiring the plugin trust path end to end — done (2026-09-26).** `plugin-host` now
   carries `PluginAdmission` (`plugin-host/src/admission.rs`): it reads
   `.nichlink/plugins/{official,user}.lock`, selects the manifest against that catalogue,
   verifies it (`verify_signed` for an official source, `verify_artifact` for a user one),
   and reports the lane the earned assurance buys. Measured before the change: **no
   in-tree code outside `core` called `verify_signed`**, and the only `PluginCatalog::parse`
   callers were `core`'s own tests and Studio's writer — so the Official lane was
   unreachable. One kernel contradiction had to go first: `PluginCatalog::contains_manifest`
   compared the three optional provenance fields (signature, key fingerprint, revocation
   list) for equality, which made a seven-field official record — what a host's own UI
   writes — unsatisfiable, because such a record can only equal a signature-less manifest
   and a signature-less official manifest is refused by every trust policy with a root.
   They are now expectations: a record that carries one pins it, a record that omits it
   leaves it to the signature check. Pins: `core`'s
   `a_record_without_provenance_fields_does_not_pin_them`, and `plugin-host`'s
   `an_official_artifact_travels_the_lock_to_the_slot`, `::a_tampered_registration_is_refused_at_the_host_seam`,
   `::an_official_artifact_absent_from_the_lock_is_refused`, `::a_revoked_version_is_refused`,
   `::an_official_artifact_without_a_trust_root_is_refused`, `::a_user_artifact_takes_the_digest_lane`.
4. **Studio's destructive-action UX** (keep a copy instead of deleting, an undo, or a
   confirmation) — still a product decision. The hand-drawn canvases are **deleted**
   (decided 2026-09-26): 1756 lines of canvas code and their geometry tests are gone,
   `rataflow` is the only call-tree drawer, the `g`/`v` keys went with them, and a build
   without `node-graph` keeps the panel and names the feature instead of drawing
   nothing.
5. **Two things the 0.1.1 pass changed** (recorded for the reader of that release): the P1
   trait change (`PluginSignatureVerifier::verify` now receives the canonical payload) is
   a breaking change for any out-of-tree host; and the new gates tighten the default
   `cargo test --workspace`, so a contributor adding an English-only doc block or an
   unregistered kernel module now gets a red build.

## Next deepening queue / 下一步深化队列

The defect queue is empty; this is the *design* queue, measured against `5c85099` after the
0.1.1 release and ranked by (unblocks real use) × (kernel or protocol depth). Items 1–7 are all
done; the queue is empty, and round 4 closed it by finishing item 4.
缺陷队列已清空；这是**设计**队列，对着 0.1.1 发布后的 `5c85099` 实测，按（能解锁真实使用）×
（内核或协议深度）排序。第 1–7 项全部完成；队列已空——第 4 轮以做完第 4 项收口。

1. **Real trace ingest — both slices done (2026-09-26).** No artifact format,
   writer, or reader existed: the only trace env read selected a *mode*
   (`run_method/src/runtime/trace/call_trace.rs`), `core`'s lexicon had zero
   `trace` tokens, and Studio installed the built-in sample unconditionally
   (`studio/src/studio/app/lifecycle.rs`), whose `NodeId::from_path` domain never
   matches the registry's `from_namespaced_path` — so `CallEvidence::Live` and the
   DATA panel could never reflect a real host. Slice 1 ships the versioned artifact
   in `run_method/src/runtime/trace/artifact/` plus `TRACE_DIR`/`TRACE_FILE`/
   `TRACE_FILE_ENV` in the kernel lexicon; slice 2 ships the loader in
   `studio/src/studio/app/trace.rs` (`TraceStatus`, `App::install_trace`, the four
   identity checks), and the built-in sample is deleted — the legend now reads
   `TRACE: none` / `LIVE` / `TRACE mismatch`. One hole was found while preparing
   slice 2 and fixed in slice 1: `write_trace_artifact` stamped its identity
   anchors from `NICH_LINK_NAMESPACE`, the *reader's* override, so a host would
   have published an artifact its own compiled ids do not inhabit; the namespace
   is now a parameter (`env!("CARGO_PKG_NAME")` for a host crate). Slice 1 added a
   cross-crate symbol, which is why the version line moved to `0.1.2`; the design
   document's residual open question is its own §4 boundary (what the trace is
   not), and its measured corrections are recorded there.
2. **MCP had no registry query — done (2026-09-26), by wiring the bridge to the build's own
   derivation.** The bridge registered only five source-text tools, so an agent re-derived the
   tree by grepping — the failure the registry exists to replace. `nichlink.registry` now
   returns the rows `nichlink_build_method::face_views` derives, the same function the CLI's
   `explain` calls, so a reported path or identity is the one the host compiled; `mcp` gained a
   `nichlink-build-method` dependency and the workspace version moved to `0.1.3` (the
   cross-crate-symbol rule below). The namespace is the real problem this item hid: a package
   name *is* the `NodeId` namespace, so the bridge resolves it as `NICH_LINK_NAMESPACE` →
   Cargo's package name → **refusal**, never the documented default, because an identity
   reported under `nichlink.default` names a node no host compiled. That read moved out of
   `cli` into `nichlink_build_method::package_name`, so the command line and the bridge share
   one authority instead of two copies. Pins:
   `mcp::registry::registry_tests::{a_face_is_reported_under_the_namespace_the_package_compiled_with,
   a_configured_namespace_wins_verbatim_before_cargo_is_asked,
   a_directory_without_a_package_is_refused_with_the_way_out}` and
   `mcp::protocol::protocol_tests::the_registry_tool_is_listed_and_answers_about_this_package`;
   the first three go red when the namespace falls back to the default, the last one when the
   tool leaves the catalog or the dispatch table. **The kernel-API alternative was rejected**:
   `face_views` parses declarations with `syn` and walks the filesystem, and the kernel may do
   neither — a kernel port would be a second, divergent derivation of the same tree, which is
   exactly what this item is about. What stays open is contract and admission data: those live
   in the built `RegistrationSnapshot`s, not in a source scan (`docs/roadmap-1.0.md` item 10).
3. **Process plugins inherit the host's environment and working directory — done
   (2026-09-26).** `ProcessLimits` gains `inherit_env` (default `true`, so an
   existing host is unchanged) and `ProcessProgram` gains `environment` and
   `current_dir`; the spawn applies them, and staging carries them to the staged
   copy. Pin: `fault_matrix::the_host_narrows_the_environment_and_the_working_directory`
   — a three-bit probe from the child itself, red when the spawn stops applying the
   knobs (`010` against `101`). The probe measures `HOME` rather than `PATH`
   because a bare `/bin/sh` invents `PATH` (`/no-such-path` on this machine) from an
   empty environment, which would have reported "inherited" for a child that
   inherited nothing. The threat model's residual risk now says what these knobs do
   and do not buy: filesystem and network access still need confinement outside
   the workspace.
4. **The source root is hardcoded to `src/`, and a missing `src/` aborts — the abort
   half is done (2026-09-26).** `build_method` still resolves the source tree as
   `package_root/src`, but the panic is gone: `pipeline::run` refuses a tree that is not
   a directory with a `face-layout` diagnostic naming it, `discovery` reports an
   unreadable directory (including one that vanishes mid-walk) instead of aborting, and
   the two paths that consume the diagnostics behave as their contracts require —
   structured callers get them back (`check --json` writes the document), the build
   script fails loudly with the rendered text. Pins:
   `build_method::missing_source_tree::{a_package_without_a_source_tree_is_reported_as_a_layout_problem,
   an_empty_source_tree_is_not_a_layout_problem}`,
   `pipeline_tests::a_build_script_without_a_source_tree_fails_with_the_layout_diagnostic`,
   and `cli::check_reports_a_source_tree_outside_src_as_a_diagnostic`; the last one was
   measured red against the old `expect` (`src directory must exist: Os { code: 2 }`).
   **Both halves are now done (2026-09-26).** The second half — a library outside `src/` —
   was measured first, and that measurement corrected this item's own suggested follow-on:
   "resolve the real target from `cargo metadata`" is exactly what the *pipeline* cannot do.
   `cargo metadata` does report the answer (for `[lib] path = "host/lib.rs"` the lib target's
   `src_path` is `<root>/host/lib.rs`), but `pipeline::run` executes **inside** a build script,
   and spawning `cargo` from there risks the lock the outer build holds — the same reason
   `package_name`'s doc says nothing in the pipeline calls it. Measured alongside it: identity
   is `manifest_relative_source` (the file relative to the manifest directory, minus one
   leading `src/`), and `file!()` for such a package is `host/print.rs`, so a face beside the
   target must be stamped `host/…`.
   The choice fell on **(b), a narrow `[lib] path` read with a verify step**, because it is the
   only option that adds no dependency to every host's build script (a) and no new host-facing
   knob (c), and because its failure direction is loud: the read is one key in one table
   (quotes, comments, the dotted `lib.path = "…"` spelling), and a path that names no file is
   refused by name before anything is walked. The design that made it small is that the two
   hardcoded decisions are **two bases**, now resolved once in
   `build_method/src/source_layout.rs`: the *scan root* the walk reads, and the *identity base*
   an identity path is relative to. They are the same directory (`<root>/src`) for every
   package this workspace ships, and differ only for a target outside `src/`, where the scan
   root becomes the target's directory and the identity base becomes the package root — which
   is what makes `relative_display` and the `src.join(relative)` inversions keep working
   unchanged. Entry resolution is the one place that genuinely needs both, so it takes the
   layout; the conventional-entry choice moved to `build_method/src/entry_default.rs`, which
   also brought `entry.rs` from 478 lines back **under** the 450 ceiling, so its ratchet entry
   is deleted rather than enlarged.
   Pins: `build_method/tests/outside_src_layout.rs::{a_library_target_outside_src_keeps_its_identity_prefix,
   the_face_view_reports_the_same_identity_as_the_build,
   a_library_target_that_is_not_a_file_is_refused_by_name}` (the first asserts the *raw
   identity bytes* in the generated tree, and that the old `control/control.rs` identity is
   absent) and `cli::{check_accepts_a_library_target_outside_src,
   check_reports_a_library_target_that_is_missing_as_a_diagnostic}`; all of them go red when
   the manifest read is neutered, measured. `grafts_reports_a_host_whose_sources_cannot_be_read`
   was re-aimed at a bin-only package, because the `[lib] path` fixture it used is now a
   *supported* layout. Boundary kept: `[[bin]]` targets do not move the root — a package may
   have several and nothing chooses between them.
5. **MCP request framing is unbounded and `jsonrpc` is never checked — done
   (2026-09-26).** Request lines are read through a bounded frame reader
   (`MAX_REQUEST_BYTES`, 1 MiB): an over-long line is refused with `-32600` and its
   remainder drained **up to its terminator**, so the next frame survives; a
   request must carry `"jsonrpc":"2.0"`; and a member that is not a JSON object is
   answered with `-32600` instead of being dropped (a client can no longer wait
   forever on it). Pins: `an_over_long_line_is_refused_and_the_next_frame_still_answers`,
   `a_request_without_or_with_another_version_is_refused`,
   `a_member_that_is_not_an_object_is_refused_rather_than_dropped`, plus
   `a_notification_with_another_version_is_still_silent`; all four were measured red
   with their check removed. The first draft of the drain used a plain `read` into a
   scratch buffer, and the test caught it swallowing the next frame — a slice or a
   pipe hands over as much as fits.
6. **Studio can still guess a destructive target outside an explicit project context —
   done (2026-09-26), and the answer is refusal.** `studio/src/studio/app/writers.rs`
   holds the two guards every writer now goes through: `selected_package_root` (the
   selected project, or a named refusal) and `with_selected_project` (the authoring
   context a write runs in). The six authoring-context writers — delete, add, edit,
   graft create/rewrite/remove — and the plugin-lock writer use them, so the fallback
   in `package_root()` is now reachable only from reads. A launched session adopts its
   project at startup, so nothing user-visible changes; the refusal exists for a
   library caller, a test, or a cleared selection. Pins:
   `studio::app::tests::project::{a_write_without_a_selected_project_is_refused,
   a_delete_without_a_selected_project_is_refused}`, both red when the wrapper was made
   to fall back to `package_root()`.

7. **Two readers disagreed about what a package is named — measured while doing item 2, done
   (2026-09-26).** `studio/src/studio/app/namespace.rs` read the literal `[package] name` out
   of the manifest text (section-scoped, quotes and comments handled, `NICH_LINK_NAMESPACE`
   wins), while the CLI and the MCP bridge ask Cargo through
   `nichlink_build_method::package_name`. They agree on every manifest this repository ships
   and on the cases the old hand-rolled scan got wrong, but not on the TOML forms a literal
   line scan cannot see — measured on a fixture whose only name line is the dotted
   `package.name = "dotted-name"`: `cargo metadata` answers `dotted-name`, the literal reader
   answered `None`, so Studio authored that project under `nichlink.default` while a build
   named it `dotted-name` — two identity domains for the same faces, and a trace or graft
   record written on one side could not resolve on the other. Studio now takes Cargo's answer
   (one subprocess per project adoption — the adopted namespace is cached in the project
   context, so no write path pays for it), and the literal reader is deleted. Pin:
   `studio::app::tests::project::the_namespace_comes_from_cargo_not_from_a_literal_manifest_scan`,
   whose dotted case was **red against the literal reader with the real value** (`nichlink.default`
   vs `dotted-name`) before the switch, not merely in a transcription; the table's other seven
   cases keep every form the old scan had to answer. Studio keeps the documented default as its
   last resort, and that asymmetry with the bridge is deliberate: authoring *creates* a tree, so
   `nichlink.default` is a real answer for a project nobody has built yet (the new-project wizard
   depends on it), while a *query* about an existing tree must not invent an identity domain —
   which is why the bridge refuses instead. Two fixtures also gained a `src/lib.rs` target: the
   fixtures are asked about through Cargo now, and `cargo metadata` refuses a manifest with no
   target — which is what a real host has anyway.

## Next deepening queue (MCP production chain) / 下一步深化队列(MCP 生产链路)

Stated by the maintainer (2026-09-26) and ranked by (unblocks real use) × (kernel or protocol depth):
the bridge must stop being an interpreter and take over the *production chain* — graft/cut, search that
answers with tree diffs, plugins, project scaffolding, edits that land at a named file and position, and
above all an agent that *notices* when faces should share behaviour. Write semantics decided with it:
**preview first, apply on confirmation.**
维护者(2026-09-26)给出方向,按(能解锁真实使用)×(内核或协议深度)排序:桥不能只是解释器,而要接管
**生产链路**——graft/cut、能以树 diff 作答的 search、插件、项目脚手架、落到指定文件与位置的编辑,
以及最重要的:让代理**察觉**到本应共享行为的注册面。写入语义一并定下:**先预览,确认后落盘。**

1. **Write foundation — done (2026-09-26): `nichlink.apply` — `add`, `edit`, `rename`,
   `delete`, previewed.** The
   bridge called the same authoring executor Studio calls (`add_module_from_face`, `edit_module_face`,
   inside an `AuthoringContext` built from the resolved root and the Cargo-reported namespace), after
   loading the package's own faces into a `Registry` the way Studio's `load_registry` does. The preview
   is the *real* operation on a throwaway copy of the package, reporting the file diff and the tree that
   would result; `apply: true` writes and names the files. Pins:
   `mcp::apply::apply_tests::{a_preview_reports_the_resulting_tree_and_leaves_the_project_alone,
   an_apply_writes_the_face_and_its_path_can_aim_the_next_call,
   a_package_that_cannot_be_named_is_refused_with_the_way_out,
   an_action_that_is_not_implemented_is_refused_by_name}` and
   `mcp::protocol::protocol_tests::the_apply_tool_is_listed_and_dispatched`; the first three were
   measured red (preview writing into the project; apply never writing; the parent ignored) before
   being kept.
   `rename` is `edit` with `fields.module` and `delete` moves the module into the executor's
   recoverable trash; both reuse the preview, so their pins are the same shape
   (`a_delete_removes_nothing_until_it_is_applied`, `a_rename_moves_the_module_and_keeps_the_face` —
   the latter asserts the diff shows *both* halves, the file that goes and the file that arrives).
   `edit` became a **patch**: the face is read back through the new
   `nichlink_run_method::authored_face` (`AuthoredFace::as_patch`), the request's fields are
   overlaid by name — a misspelled field is refused, not dropped — and the complete set goes to the
   executor. Without that, a two-field request blanks the other twenty-two, which is what the
   executor's "rebuild from what you are given" contract means; that pin is
   `a_partial_edit_keeps_the_fields_it_does_not_name`, measured red against a request that skips the
   read-back.
   Mistakes the pins caught while being written (all three were real, none were cosmetic): the
   reported path belonged to the deleted copy rather than the project; the default parent was the
   bare `ROOT_NODE_ID` — a *different* identity from the namespaced root, which would have hung a
   face under a tree no host compiled while still reporting a `root/…` path; and the edit's
   read-back ran **outside** the `AuthoringContext`, so it resolved the face against whatever
   directory the process happened to be in — invisible in a CLI-shaped manual run (which has
   `NICH_LINK_PACKAGE_ROOT` set) and caught by the unit test, which has no such variable.
2. **Tree diffs as answers**: `nichlink.diff` over the source tree, the graft overlay, a record, or an
   MIR snapshot, and `nichlink.search` gaining a tree-level delta instead of only source text.
3. **The consistency analysis**: a kernel-pure report over `RegistrationSnapshot`s — which faces
   differ only in the fields that should make them differ — so an agent is told what to unify instead
   of being asked to notice. This is the piece that makes "接管" proactive rather than obedient.
4. **graft/cut writes**, **plugin admission**, and **`nichlink.new`** (scaffolding, with the naming
   gate's template constraints and explicit overwrite semantics).

**Measured but not worth doing next:** the nightly `rustc_driver` MIR adapter and
monomorphization work (they depend on the evidence interchange above), shrinking the
450-line ratchet's nine entries (hygiene), the `wasmi` 2.0 upgrade (1.1.0 already carries
the sandbox fixes), the 29 intentional `ignore` doc fences, and freezing the public surface
for 1.0 (the version line deliberately stays 0.1.x).
**实测但不值得下一步做：** nightly `rustc_driver` MIR 适配器与单态化工作（依赖上面的证据互通）、
缩小 450 行棘轮的九条欠账（卫生）、`wasmi` 2.0 升级（1.1.0 已带沙箱修复）、29 条有意为之的
`ignore` 文档围栏、以及为 1.0 冻结公开面（版本线有意留在 0.1.x）。

## Method note / 方法说明

**A new cross-crate public API makes the package audit red until the next release.**
`tools/nichlink-package-audit` builds each packaged tarball in isolation, so its internal
dependencies resolve from crates.io — the *published* versions. When the checkout adds a
symbol that another crate then uses (measured: `build_method::is_checkout` used by `cli`),
`cargo package` of the calling crate fails to compile even though the workspace is green,
and it stays red until a new version is published. Two consequences for this queue: a fix
that needs a new cross-crate symbol has to be paired with a version bump, and P1 (below)
is exactly such a fix.
**Version line, re-measured 2026-09-26:** the rule below bites when a *published* version exists without
the symbol. The whole write path (`nichlink.apply`, and the `AuthoredFace`/`authored_face` reader it
needs) landed inside `0.1.3`, which is still **unreleased**, so no new version was needed to keep the
audit in its designed state — measured: `contents checked: all 9`, `verified: nichlink-core`,
8 skipped, exit 0. A bump is required when the symbol must be distinguishable from one already on the
index; it is not a ritual.
**版本线,2026-09-26 重测:** 下面的规则在"已发布版本缺少该符号"时才咬人。整条写入路径
（`nichlink.apply` 与它需要的 `AuthoredFace`/`authored_face` 读取方）都落在**尚未发布**的
`0.1.3` 里,因此不需要新版本就能让审计留在设计中的状态——实测:`contents checked: all 9`、
`verified: nichlink-core`、8 skipped、exit 0。需要与 index 上已有的版本区分时才必须推进;它不是仪式。
**新增跨 crate 公开 API 会让包审计红到下个版本。**
`tools/nichlink-package-audit` 在隔离环境里构建每个包，因此它的内部依赖从 crates.io 解析——也
就是**已发布**的版本。当检出新增一个随后被别的 crate 使用的符号时（实测：
`cli` 使用 `build_method::is_checkout`），调用方的 `cargo package` 会编译失败，尽管工作区全绿，
而且它会一直红到发布新版本。对本队列有两点后果：需要新增跨 crate 符号的修复必须与版本线推进一起做，
而 P1 恰恰就是这种修复。


Every angle pinned `d72c703` and mutated a snapshot under `/tmp`, so none of them wrote to
this checkout — but two reported that the *live* tree was being modified while they read it
(this writer). Their line numbers are valid for `d72c703`; a later round working from this
index should re-measure before editing.
每一路都钉住 `d72c703` 并在 `/tmp` 快照上变异,因此都没写本检出——但有两路报告说它们阅读期间
**活**树正在被修改(就是本写者)。它们的行号对 `d72c703` 有效;后续轮次据此索引动手前应先重测。
